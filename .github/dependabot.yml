version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    groups:
      all:
        patterns:
          - "*"
    schedule:
      interval: "monthly"
    cooldown:
      default-days: 7
  - package-ecosystem: "npm"
    directory: "/"
    groups:
      all:
        patterns:
          - "*"
    ignore:
      # Node.jsのメジャーバージョンは変更しない
      - dependency-name: "@types/node"
        update-types: ["version-update:semver-major"]
      # dependabotがアップデート失敗するため、vitestのサブパッケージは対象としない
      - dependency-name: "@vitest/*"
    schedule:
      interval: "weekly"
    cooldown:
      default-days: 7
